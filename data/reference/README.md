# Reference data

This folder contains small supporting datasets used by the application.

Examples include:

- lookup tables;
- place-name lists;
- district names;
- variable labels;
- reference classifications;
- non-raster spatial layers used for context.

Climate rasters should not be stored here. They belong in the `rasters/` directory and are referenced through `config/raster_catalogue.csv`.
